<schemalist>
  <schema id="org.merproject.btts" path="/org/merproject/btts/">

    <key name="adapter" type="s">
      <default>"hci0"</default>
      <summary>Bluetooth adapter to work with</summary>
      <description>
        Specified with HCI device name (e.g. "hci0".)
      </description>
    </key>

    <key name="host-alias" type="s">
      <default>""</default>
      <summary>Name to be always used by the selected adapter</summary>
      <description>
        This value will be used for the org.bluez.Adapter1.Alias bluez5 D-Bus
        API property of the adapter selected with the key "adapter".
      </description>
    </key>

    <key name="device" type="s">
      <default>""</default>
      <summary>Remote Bluetooth device to work with</summary>
      <description>
        Specified with Bluetooth device address.
      </description>
    </key>

  </schema>
</schemalist>
